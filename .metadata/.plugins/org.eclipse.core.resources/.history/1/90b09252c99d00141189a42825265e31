#include "DriveSystem.h"

DriveSystem::DriveSystem(){

	stick  = new Joystick(0);
	stickTwo = new Joystick(1);
	talZeroEnco = new Encoder(5, 6);
	talTwoEnco = new Encoder(7, 8); //Change inputs for both of these.
	talZero = new Talon(0);
	talTwo = new Talon(2);
	drive = new RobotDrive(talZero, talTwo);

}

DriveSystem::~DriveSystem(){

	delete stick;
	delete stickTwo;
	delete talZeroEnco;
	delete talTwoEnco;
	delete talZero;
	delete talTwo;
	delete drive;

}

void DriveSystem::DriveArcadeStyle(){

	sReadX = stick->GetX();
	sReadY = stick->GetY();
	sReadZ = stick->GetZ();
	eRead = talZeroEnco->GetRaw();
	eReadTwo = talTwoEnco->GetRaw();

	if((sReadX == 0.0 && sReadZ == 0.0) && (sReadY > 0.0 || sReadY < 0.0)){

		//TODO: Find values for joystick readings so we can decide how much variation from joystick straight will be interpreted as straight
		if(eRead < eReadTwo){

		}

		else if(eRead > eReadTwo){

		}

		else{

		}

	}

	else{

		drive->ArcadeDrive(*stick, false);

	}

}

void DriveSystem::DriveMecanumStyle(){

}

void DriveSystem::DriveTankStyle(){

}
